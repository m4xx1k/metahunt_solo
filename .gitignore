node_modules
dist
.pnpm-store
.env
.dev-admin.jwt
.DS_Store
.codex
.scratch
.omc
# .claude is local state except skills, which are versioned project tooling
.claude/*
!.claude/skills/

# Vercel CLI artifacts (created by `vercel link` / `vercel pull`)
.vercel

# Next.js build output (apps/web/.gitignore covers this too — belt and suspenders)
apps/*/.next
apps/*/next-env.d.ts
apps/*/tsconfig.tsbuildinfo
apps/*/.env*.local

# Business strategy, funnel metrics, session handoffs, drafts. Local-only:
# the repo is public, so anything here would be published. See md/README.md#hygiene.
.private/

# Personal notes (not engineering docs) — keep out of the project repo
diploma.md
article.md
docs/runbook/etl-stream-blocks-ua.md
docs/runbook/etl-temporal-stream-demo.md
THESIS_TODO.md

# Stale Prisma-era artifacts kept locally for reference, not committed
offy-db/
backups/

# business/session docs — never commit
.private/

# Local screenshot scratch (visual checks, never a deliverable)
scripts/.screenshots/
