# Releases / journal

Chronological log of changes. Don't duplicate git log — write what's useful for onboarding and understanding how the project evolved.

Format: group by date, short bullets inside. If a bullet has bigger context, link to an ADR / docs / PR.

---

## 2026-04-26

- Monorepo scaffold on pnpm workspaces. `apps/etl` (`@metahunt/etl`) + `libs/database` (`@metahunt/database`, a `@Global()` Nest module with a placeholder constant). The old `_metahunt/` (NestJS CLI monorepo) is archived as a read-only reference. → ADR-0001
- `apps/etl` switched from headless `createApplicationContext` to a full HTTP server via `@nestjs/platform-express`. `GET /` returns `{ greeting }` from the lib — a canary check that DI across workspaces works. → ADR-0002
- Added dev scripts: `pnpm dev` at the root (parallel `tsc -w` in the lib + `nest start --watch` in the app), plus `start:prod` and `start:debug`.
- Set up engineering documentation (`md/`) using Snapshot + Journal layout.
- Added package-level `README.md` for `@metahunt/etl` and `@metahunt/database` as front doors (what the package is, public surface, run/build commands). Documented the rule in `md/README.md`: packages do not get their own `md/` folders; everything stays centralized at the repo root until a package outgrows that.
- Migrated `@metahunt/database` from placeholder token to real Drizzle + Postgres provider (`DRIZZLE`), with schema for `sources`, `rss_ingests`, `rss_records`, migrations, and seeds.
- ETL health endpoint now verifies DB connectivity (`SELECT 1`) and returns `{ "status": "ok", "db": "up" }`.
- Unified local/server env behavior for ETL startup: process-level env is primary; local root `.env` is loaded via `node --env-file-if-exists=../../.env`.
- Removed migration drift artifact `0004_purple_exodus` and documented migration hygiene: schema changes must be generated with synced `migrations/meta/*`.
- Added Railway deployment IaC for the pnpm workspace setup: root `Dockerfile` (multi-stage build on Node 22) and `railway.json` with `DOCKERFILE` builder, pre-deploy migrations, and explicit start command.
- Added root `.dockerignore` to keep deployment context clean and deterministic.
- Added first runbook entry for production deploy flow and required Railway variables. → `md/runbook/railway-deploy.md`
- Initialized a fresh git repository in `~/plan-a/metahunt`, created the root commit, and aligned local commit author with `m4xx1k` identity.
- Configured git remote to `git@github-m4xx1k:m4xx1k/metahunt_solo.git`, pinned repo-level SSH command to `~/.ssh/id_m4xx1k`, and pushed `main`.
- Fixed Railway Docker build failure (`tsc: not found`) by forcing recursive workspace install with dev dependencies in Docker (`pnpm install -r --frozen-lockfile --prod=false`) so package-level build tools are available during image build.
- Hardened Docker build flow for Railway by running recursive workspace install in the `build` stage (after full source copy) and copying `node_modules` from `build` to `runtime`, so workspace binaries (`tsc`) resolve reliably during CI builds.
- Fixed Railway pre-deploy migration runtime error by copying `tsconfig.base.json` into the runtime image, required by `ts-node` during `libs/database/migrate.ts`.
- Fixed runtime module resolution for pre-deploy migration by copying workspace `node_modules` for `libs/database` (and app workspace deps for `apps/etl`) into the runtime image.
- Added operational Railway runbook details: CLI flow for `intelligent-harmony`, Postgres wiring via Railway variable reference, deploy vs redeploy semantics, and watch-pattern caveats for root infra files.
- Enforced Railway `watchPatterns` via `railway.json` to include root infra files (`Dockerfile`, `railway.json`, lockfile, package manifest) plus `apps/etl/**` and `libs/**`, preventing skipped deployments on infra-only commits.
- Added Railway healthcheck path (`/`) in `railway.json` so deployment health validation tracks the ETL HTTP endpoint explicitly.
- Tightened docs for day-to-day usage: Railway runbook now has explicit operational rules, and roadmap moved Stage 03 (env/config + deploy baseline) to done with Stage 04 as current focus.

---

## 2026-04-28

- RSS+Temporal port — T3 (StorageModule + MinIO). Ported `apps/etl/src/storage/{storage.module,storage.service}.ts` from legacy under TDD: spec-first with `aws-sdk-client-mock`, two cases covering `upload`/`download`. Added `@aws-sdk/client-s3` runtime dep, `aws-sdk-client-mock` dev dep. `docker-compose.yml` gained `minio` and a one-shot `minio-init` (`minio/mc`) sidecar that creates the `rss-payloads` bucket idempotently. Root `db:up` widened to `docker compose up -d` so all local infra comes up together. → [migration tracker](migrations/_done/rss-temporal.md)
- RSS+Temporal port — T4 (Temporal in compose + SDK deps). Added `temporalio/auto-setup:1.26` (sharing the existing `db` Postgres) and `temporalio/ui:2.34.0` to compose. ETL `package.json` now includes `nestjs-temporal-core` + `@temporalio/{client,worker,workflow,activity}` runtime deps and `ts-loader` dev dep (for the workflow webpack bundler in T12). UI reachable at `:8080`; both `default` and `temporal-system` namespaces visible. → [migration tracker](migrations/_done/rss-temporal.md)
- RSS+Temporal port — locked two previously-open risks. `workflowsPath` in `RssModule` will use `resolve(__dirname, 'workflows')` (works for spec/dev/prod, no `process.cwd()` dependency). Workflow `.ts` source is delivered to runtime via a `nest-cli.json` assets rule that copies `rss/workflows/**/*.ts` next to compiled `rss.module.js` in `dist/` — the standard `Dockerfile` already ships `dist/`, so no special `COPY` is needed.
- RSS+Temporal port — T5 (RssFetchActivity + spec). First Temporal activity ported under TDD: `apps/etl/src/rss/activities/rss-fetch.activity.{ts,spec.ts}`. Standard path-alias rewrites applied (`@app/database` → `@metahunt/database`, `import { schema }` from the barrel); body unchanged from legacy. Spec hand-rolls a chained Drizzle mock (`select`/`insert`/`update` builder), mocks `StorageService.upload`, `@temporalio/activity#activityInfo`, global `fetch`, and `node:fs/promises#readFile`. Three cases cover the activity's branches: `rssUrl` + fetch ok → uses fetched body; `rssUrl` + fetch !ok → falls back to local `${code}-rss.xml`; no `rssUrl` → fallback. Suite total now 5/29 (was 4/26). → [migration tracker](migrations/_done/rss-temporal.md)
- RSS+Temporal port — T6 (RssParseActivity + spec). Second Temporal activity ported under TDD: `apps/etl/src/rss/activities/rss-parse.activity.{ts,spec.ts}`. Same path-alias rewrites; body unchanged. Spec wires the **real** `RssParserService` (covered by T2) alongside mocked `DRIZZLE` and `StorageService.download`; chained Drizzle mock ends in a `returning` step that resolves to the inserted-id list. Synthetic in-test XML mixes 3 IT titles with 1 blacklisted "Recruiter" so the filter is exercised on the way through. Two cases: all-new → 3 rows inserted with hashes matching `parser.computeHash`; partial dedup → existing-hash-set blocks the first item, only the remaining two are written. Suite total now 6/31 (was 5/29). → [migration tracker](migrations/_done/rss-temporal.md)
- RSS+Temporal port — T7 (RssExtractActivity + extractor abstraction, OpenAI default). **Decision change vs. legacy:** the activity no longer talks to a vendor SDK directly. New `apps/etl/src/extraction/` module introduces a `VacancyExtractor` interface (`extract(text) → ExtractedVacancy`) behind the `VACANCY_EXTRACTOR` token, with two impls: `PlaceholderVacancyExtractor` (static shape) and `OpenAiVacancyExtractor` (`openai@^4` chat.completions + a single `extract_vacancy` function tool, parsed through the existing zod schema). `ExtractionModule` picks the impl at bind time via `LLM_EXTRACTION_ENABLED`; future swap (e.g. BAML) = drop in a new `VacancyExtractor` impl, no changes to activity/workflow/tests. The activity itself is now ~25 lines: select record → delegate to extractor → write `extractedData` + `extractedAt`. Env validation reworked: `ANTHROPIC_API_KEY`/`ANTHROPIC_EXTRACTION_ENABLED` removed, `OPENAI_API_KEY` + `OPENAI_MODEL` (default `gpt-4o-mini`) + `LLM_EXTRACTION_ENABLED` added, with the same "key required when flag on" guard. Three new specs under TDD: openai extractor (tool_call ok / no tool_call → throws), activity (delegate happy path / record missing → throws). → [migration tracker](migrations/_done/rss-temporal.md)
- RSS+Temporal port — T8 (RssFinalizeActivity + spec). Trivial port: `apps/etl/src/rss/activities/rss-finalize.activity.{ts,spec.ts}`. Update of `rss_ingests` with `status` + `finishedAt`, conditional `errorMessage` spread (no overwrite on success). Two spec cases assert the `update().set()` payload shape. → [migration tracker](migrations/_done/rss-temporal.md)
- RSS+Temporal port — T9 (rss-ingest workflow). Copied `apps/etl/src/rss/workflows/rss-ingest.workflow.ts` verbatim from legacy — pure `@temporalio/workflow#proxyActivities` + type-only activity imports, no rewrites. Build verified via `pnpm exec nest build`; `dist/rss/workflows/rss-ingest.workflow.js` lands cleanly. Webpack workflow-bundler at worker boot is exercised in T12.
- RSS+Temporal port — wave summary (T7+T8+T9). Suite total now 9 suites / 37 tests (was 6/31). Pipeline activities are all in place; remaining: scheduler (T10), controller (T11), `RssModule` wiring + AppModule smoke override for `autoStart` (T12), E2E smoke (T13).

---

## 2026-04-29

- RSS+Temporal port — T10 (`RssSchedulerService`). **API redesign vs. legacy:** replaced `ingestAll(local: boolean)` with two self-documenting methods — `ingestRemote()` (filters `where(isNotNull(rssUrl))`, the cron use-case once `@Cron` is re-enabled) and `ingestAll()` (every source, the dev / HTTP-trigger use-case). Shared logging + `temporal.startWorkflow` loop lives in a private `startWorkflows(sources)` helper. Spec uses a chained-builder DRIZZLE mock with separate `executeViaWhere` / `executeDirect` jest.fns so each test asserts the *path* taken, not just the result. → [migration tracker](migrations/_done/rss-temporal.md)
- RSS+Temporal port — T11 (`RssController`). `GET /rss` now returns `202 Accepted` with body `{ triggered: "all" }` and fire-and-forget `void scheduler.ingestAll()`. The 202 + body change vs. legacy makes the curl smoke informative instead of returning empty 200. Spec verifies it calls `ingestAll` (not `ingestRemote`) exactly once with no args.
- RSS+Temporal port — T12 (`RssModule` wired into `AppModule`). Locked decisions applied: dropped `DatabaseModule.forRoot()` from RssModule's imports (already global at AppModule level), added `ExtractionModule` (provides `VACANCY_EXTRACTOR` for `RssExtractActivity`), `workflowsPath: resolve(__dirname, "workflows")`, webpack `ts-loader` hook for workflow bundling. AppModule smoke spec extended to assert all RSS providers + `RssController` + `HealthController` resolve. **Open-item resolved:** `autoStart` now gated on `NODE_ENV !== 'test'` (jest sets `NODE_ENV=test` by default), so the AppModule spec compiles the graph without dialing Temporal.
- Workflow bundler fix — `workflowsPath` pointed at the `workflows/` directory, but Temporal's autogen entrypoint emits `require(<dir>)` and webpack default resolution needs an `index.{ts,js}`. Without one the build dies with `Module not found: Can't resolve '<workflowsPath>'`. Fix: added `apps/etl/src/rss/workflows/index.ts` as a barrel re-exporting `./rss-ingest.workflow`, matching the canonical Temporal Cloud + NestJS pattern (per Temporal community discussion). Locked decision in tracker.
- ETL `main.ts` env loading — Production scripts use `node --env-file-if-exists=../../.env`, but `nest start --watch` does not, so `pnpm start:dev` failed env validation locally. Added explicit `dotenv.config({ path: resolve(__dirname, "../../../.env") })` in `main.ts` (ts-resolved into `apps/etl/dist/`, so the path lands on the repo `.env`). `dotenv` does not override existing process env, so prod runtime is unaffected. Added `dotenv` to `apps/etl/package.json` runtime deps.
- Health endpoint — added aggregated `GET /healthz` (`apps/etl/src/health/health.controller.ts`). Runs Postgres `SELECT 1`, `StorageService.ping()` (new `HeadBucketCommand`), and `TemporalService.getHealth()` in parallel via `Promise.all` with per-call `try/catch` and `latencyMs` capture. Returns `200 { status:"ok", checks: {...} }` when all green, `503 { status:"degraded", checks: {...} }` otherwise. To make this work without re-registering Temporal, `TemporalModule.registerAsync({ ..., isGlobal: true })` was set in `RssModule`. `StorageModule` added to `AppModule.imports` so `HealthController` can inject `StorageService` without going through `RssModule`. Three spec cases (all-healthy / postgres-down / temporal-degraded). Switched Railway `healthcheckPath` from `/` to `/healthz`.
- Temporal Cloud support — `env.validation.ts` gained optional `TEMPORAL_API_KEY`. When set, `RssModule` automatically enables `tls: true` + API-key auth on the connection (Temporal Cloud's API-key mode); when empty, plaintext to `localhost:7233` (local dev). No code change required for AWS S3 / Cloudflare R2 — `StorageService` already speaks generic S3 with `forcePathStyle: true`; just point `STORAGE_*` envs at the provider endpoint.
- Suite total now 12 suites / 43 tests (was 9/37 after T9). Stage 04 is one task away from done — T13 closes when the first Railway deploy lands and a `curl /rss` produces completed `rssIngestWorkflow` runs in Temporal Cloud.

---

## 2026-05-01

- BAML vacancy extractor lands as the **single source of truth** for extracted shape, prompt, and per-field rules. Added `@boundaryml/baml@^0.222` to `apps/etl` and a new `apps/etl/baml_src/`:
  - `generators.baml` → output to `../src` (BAML adds the `baml_client` subfolder),
  - `clients.baml` defining one `OpenAIClient` from `env.OPENAI_API_KEY` / `env.OPENAI_MODEL`,
  - `extract-vacancy.baml` with six enums (`Seniority`, `WorkFormat`, `EmploymentType`, `EnglishLevel`, `Currency`, `EngagementType`), three nested classes (`ExtractedLocation`, `Skills`, `Salary`), the 14-field `ExtractedVacancy`, the `ExtractVacancy(text)` function, and a baseline `test` block. Every field carries `@description("…")` with extraction rules (yearly→monthly salary, "Architect"→PRINCIPAL, hard-skills-only, etc.) which the LLM sees via `{{ ctx.output_format }}`.
  - Generated TS client lives in `apps/etl/src/baml_client/` (committed). New scripts: `pnpm --filter @metahunt/etl baml:generate` and `baml:check`.
  → ADR-0004
- **Schema redesign** — old shape (snake_case flat: `salary_min`, `work_format`, `skills: string[]`, `specialization`) replaced by the new shape (camelCase, nested: `salary.{min,max,currency}`, `skills.{required[],optional[]}`, `locations: {city,country}[]`, plus `role`, `domain`, `engagementType`, `companyName`, `hasTestAssignment`, `hasReservation`). Old rows in `rss_records.extracted_data` keep their old shape as dormant jsonb (no reader exists yet); a normalizer + re-extract job will land in Stage 06.
- **OpenAI extractor removed.** `apps/etl/src/extraction/openai.extractor.{ts,spec.ts}` and the Zod schema `apps/etl/src/extraction/extracted-vacancy.ts` are deleted. The `openai` npm package is no longer a dependency. `EXTRACTOR_PROVIDER ∈ {baml, placeholder}` (was `{openai, baml, placeholder}`); `LLM_EXTRACTION_ENABLED` is gone. `.env.example` updated.
- `BamlVacancyExtractor` is one line: `return b.ExtractVacancy(text)`. The Zod re-validation step that existed in the v1 wrapper is removed — BAML's Schema-Aligned Parsing already returns a structurally-validated value, and the BAML class is the contract.
- `PlaceholderVacancyExtractor` returns the new shape with the BAML enum values (`Seniority.SENIOR`, etc.) so the activity contract is consistent across providers.
- Tests — `baml.extractor.spec.ts` simplified to two cases (happy path, propagated client error); the schema-drift case is gone with the Zod boundary. `rss-extract.activity.spec.ts` updated for the new shape. Suite total: 12 suites / 43 tests.
- Real-world fixture for prompt iteration — captured a DOU.ua RSS item (Senior Full Stack JS at talanovyti agency, 2026-04-24) twice: as a TS module at `apps/etl/src/baml-extraction/__fixtures__/dou-fullstack-talanovyti.ts` (importable from unit tests, mirrors the `Title: …\n\n<description>` shape `RssExtractActivity` feeds the extractor) and as a `test dou_fullstack_talanovyti { … }` block inside `extract-vacancy.baml` (runnable via `baml-cli test` or the VS Code BAML extension's inline "▶ Run test"). Smoke fixture `senior_backend_remote` kept alongside it for fast iteration.

## 2026-05-03

- Daily RSS ingest now runs on a Temporal Schedule, not Nest's `@Cron`. `RssSchedulerService.onApplicationBootstrap` (re)installs schedule `rss-ingest-hourly` on every boot — calendar `{ minute: 0, hour: { start: 6, end: 22, step: RSS_INGEST_INTERVAL_HOURS } }` in `Europe/Kyiv`, overlap policy `SKIP`. The action starts a new `rssIngestAllWorkflow` which calls activity `listRemoteSourceIds` then fans out one `rssIngestWorkflow` child per source with `parentClosePolicy: ABANDON`. Existing `GET /rss` HTTP trigger and `ingestRemote/ingestAll` methods kept for manual runs.
- New env var `RSS_INGEST_INTERVAL_HOURS` (default 1, range 1..16) — change and restart the app to re-apply via the schedule handle's `update`.
- New activity `RssListSourcesActivity` + new workflow `rss-ingest-all.workflow.ts` (added to the `workflows/` barrel so the worker bundler picks them up). Suite: 13 suites / 48 tests, all green.
- Extraction is now **per-record best-effort** in `rssIngestWorkflow`: `Promise.allSettled` instead of `Promise.all`, so one bad vacancy (e.g. an LLM that mis-nests required fields) no longer fails the surrounding good records. Failure count is logged and recorded in `rss_ingests.error_message` as `extracted=X/Y (failures=N)`; `status` stays `completed` whenever fetch+parse succeeded.
- Activity retry policies normalized to `maximumAttempts: 3` with `5s/10s/20s` exponential backoff for `fetchAndStore`, `parseAndDedup`, `extractAndInsert`. `finalizeIngest` keeps 5 attempts (idempotent and critical for status correctness).
- BAML: `hasTestAssignment` and `hasReservation` switched from `bool` → `bool?`. Mini models occasionally lose scope on deeply-nested tail fields and omit them; nullable accepts that and unblocks the rest of the extraction. → regenerated `src/baml_client/`.
- Added `md/runbook/failure-recovery.md` — per-activity diagnosis + restore procedure for fetch / parse / extract failures, including how to pause the schedule during incidents.
- New endpoint `POST /rss/extract-missing?limit=N` — synchronous in-process backfill that re-runs `extractAndInsert` for `rss_records WHERE extracted_at IS NULL`, oldest first. Default 100, max 500. Recovers records that parse inserted but a prior failed run never extracted (dedup hides them from the schedule's next firing). Returns `{ attempted, succeeded, failed }`.
- Child workflow IDs are now human-readable: `rss-ingest-<code>-<YYYY-MM-DDTHH-MM-SSZ>` (e.g. `rss-ingest-djinni-2026-05-03T14-29-13Z`) instead of UUID-based. Activity `listRemoteSourceIds` → `listRemoteSources` returning `{ id, code }[]`; the workflow uses `code` for the ID and `workflowInfo().startTime` for the timestamp. Parent workflow ID stays `rss-ingest-all-<auto-suffix>` (the schedule already appends a date).
- Frontend imported into the monorepo as `@metahunt/web` (`apps/web/`) — clean copy from the standalone `metahunt-client` repo, no git history transfer. Renamed package, dropped `package-lock.json` in favor of the shared `pnpm-lock.yaml`. landing.pen moved to `apps/web/design/`. Backend Docker build is unaffected (Dockerfile copies selectively + `.dockerignore` adds `apps/web` for defense in depth). → ADR-0005, [migration tracker](./migrations/_done/frontend-migration.md), PR #4.
- Vercel deploy approach course-corrected post-merge: original plan was to **reconnect** the existing `metahunt-client` Vercel project to the monorepo; switched to **new Vercel project + sequential domain migration** (~30s downtime, no DNS propagation since records keep pointing at Vercel's edge). Cleaner separation, old project's deploy history preserved. Runbook renamed `vercel-reconnect.md` → `vercel-deploy.md` and rewritten under the new flow.
- Root `package.json` scripts gained per-app variants: `dev:etl`, `dev:web`, `build:web`, `build:all`, `start:web`, `lint:web`. Existing `dev` (parallel both), `build` (etl+database, Railway-compatible), and `start`/`start:prod` (etl, Railway compatibility) unchanged. `scripts/dev-stop.sh` learned to kill `next dev`/`next-server` processes too.
- Read-only `/monitoring` API on ETL — six endpoints under `MonitoringModule` (`apps/etl/src/monitoring/`): `GET /monitoring/{stats,sources,ingests,ingests/:id,records,records/:id}` with offset/limit pagination and inline query parsing matching the `/rss` style. CORS enabled globally with `origin: "*"` so the web app can read it cross-origin in dev and from Vercel previews. No auth — operational observability, intended to be gated behind basic-auth or an env-token before exposed publicly. PR #5.
- First frontend → backend integration lands on `apps/web` consuming `/monitoring`. Establishes the API-client pattern future endpoints will reuse: hand-written response types in `apps/web/lib/api/monitoring.ts` (per ADR-0005, no `libs/contracts` until the second integration), Server-Component `fetch({ cache: "no-store" })` for parallel `Promise.all` of stats + sources + ingests + records, all filter state in URL `searchParams` (back/forward + share), one client island for the filter form and another for `router.refresh()`. New env var `NEXT_PUBLIC_API_URL` declared in `apps/web/.env.example`. Page at `/monitoring` shows stat cards + per-source health + jsonb-aware `RssRecordCard` feed; the card mirrors `GoldenJobCard` styling and reads BAML-extracted fields (role/seniority for the title, salary, english level, skills, hasTestAssignment, hasReservation) via `apps/web/lib/extracted-vacancy.ts` (hand-mirrored from `apps/etl/baml_src/extract-vacancy.baml`). Sub-pages: `/monitoring/records/[id]` and `/monitoring/ingests/[id]`. Backend follow-up: `MonitoringService.listRecords` now selects `description` so the card has it without a per-row detail fetch. Landing nav got a "моніторинг" link.

## 2026-05-05

- Silver-layer loader pipeline shipped on `feat/loader-pipeline` (T1–T18). Six new tables (`companies`, `company_identifiers`, `nodes`, `node_aliases`, `vacancies`, `vacancy_nodes`) + seven enums project the bronze `rss_records` into one row per `(source_id, external_id)` vacancy. `external_id` is now derived per-source via pure extractors (`apps/etl/src/loader/external-id/extractors/{djinni,dou}.ts`) at parse time and locked `NOT NULL`; items whose URL doesn't match are dropped from bronze rather than poisoning the dedup key. → [migration tracker](./migrations/_done/loader-pipeline.md), [runbook](../runbook/loader-pipeline-smoke.md).
- New module `apps/etl/src/loader/`: `CompanyResolverService` (per-source identifier with slug fallback), `NodeResolverService` (race-safe alias-keyed lookup, all new nodes land in `status='NEW'` for later moderation), `VacancyLoaderService` (transactional vacancy upsert + `vacancy_nodes` rewrite from `extractedData`), `LoaderBackfillService`, `LoadVacancyActivity`. Strict alias matching only — no fuzzy / no semantic; cross-source dedup is a deferred initiative.
- New workflow `vacancyPipelineWorkflow(rssRecordId)` runs `loadVacancy` today; future stages (dedup, telegram fan-out) append without rewrite. `rssIngestWorkflow` now starts one `vacancyPipelineWorkflow` child per successfully extracted record with `parentClosePolicy: ABANDON`, deterministic `workflowId = vacancy-pipeline-<rssRecordId>`, and `WorkflowIdReusePolicy.ALLOW_DUPLICATE_FAILED_ONLY` so a failed pipeline retries on the next ingest pass. Worker generalized to bundle workflows from `apps/etl/src/workflows/index.ts` (aggregates `rss/workflows` + `loader/workflows`).
- `POST /loader/backfill?limit=N` recovers extracted records that never landed in `vacancies` (e.g. when `vacancyPipelineWorkflow` failed and the workflow-id reuse policy blocks a fresh attempt). Mirrors the `/rss/extract-missing` shape.
- Suite: 25 suites / 99 tests; full-DB smoke at `apps/etl/scripts/loader-smoke.ts` (drops + recreates an isolated `metahunt_loader_smoke` DB end-to-end). One-off backfill helper at `apps/etl/scripts/backfill-rss-external-id.ts` already populated all 86 local `rss_records` to numeric IDs before locking the column NOT NULL.

## 2026-05-06

- Silver-layer `fill-vacancies` CLI shipped (`apps/etl/scripts/fill-vacancies.ts`) — walks bronze records that extracted but never landed in `vacancies` and runs `VacancyLoaderService` per id with streaming progress (per-batch elapsed + rate). Same predicate as `POST /loader/backfill`, but bypasses Nest/HTTP for one-off runs against any `DATABASE_URL`. Refuses to run if silver tables aren't present. Verified locally: 86 bronze → 86 vacancies, 35 unique companies (heavy company dedup), 521 nodes auto-minted as `status='NEW'`, ~34 rec/s. Re-run is a clean no-op. → commit `ca6908d`.
- Taxonomy seed reworked to be type-aware. `node_aliases` PK / unique constraint moved from `(name)` → `(name, type)` (migration 0006), denormalised `type` column added, `NodeResolverService` filters by both at lookup. Seed `libs/database/seeds/data/nodes.json` populated with 216 SKILL + 21 DOMAIN + 80 ROLE canonicals and 776 aliases, all `VERIFIED`. → commit `51223fc`, [migration tracker](./migrations/taxonomy-curation.md).
- `fill-vacancies` rewritten with taxonomy coverage instrumentation. Probes `node_aliases` *before* the loader runs and classifies every `(type, name)` extracted mention as `matched-VERIFIED` / `matched-NEW` / `created-NEW`; emits a top-N frequency-weighted gap list per type — direct input for the next pass on `nodes.json`. Same commit fixes a `vacancy_nodes` PK collision: distinct alias spellings of the same skill (`"react"` / `"react.js"` / `"reactjs"`) now collapse to one node id via the seed, but the loader was emitting duplicate `(vacancy_id, node_id)` rows when two spellings appeared in the same vacancy (failed 14 / 86 records on the first post-seed fill). Loader now dedups by node id before insert, preferring `required=true` when both flags are seen. → commit `921b4e1`.
- Read-only taxonomy moderation API. New `apps/etl/src/taxonomy/` module exposes four `/admin/taxonomy/*` endpoints — `coverage`, `queue?type=…`, `nodes/:id`, `nodes/:id/fuzzy-matches`. Trigram suggestions use a per-type threshold matrix (ROLE/DOMAIN `minLen=4 minSim=0.55`; SKILL `minLen=3 minSim=0.65 minWordSim=0.5` — extra `word_similarity` gate respects token boundaries) and a length floor that suppresses `"C" → "C++"` punctuation false positives. Migration `0007` enables `pg_trgm` (was previously enabled out-of-band on dev DBs). No auth — gate before exposing beyond localhost. Phase 2 (moderator write-path) deferred. → commit `b6e1052`, [migration tracker](./migrations/taxonomy-curation.md).
- First gap-driven `nodes.json` iteration (Tier 1: aliases + new entries blocking ≥2 vacancies each). Coverage delta against 142 loaded vacancies — ROLE verified 64.1 % → 97.2 %, DOMAIN 53.5 % → 88.0 %, SKILL 53.0 % → 63.4 %, fully-VERIFIED vacancies 5 → 13 (3.5 % → 9.2 %, 2.6×). SKILL is long-tail-dominated; further iterations are gated on prompt-tuning to reduce extraction noise (see `todo/baml-extraction-prompt-tuning.md`). Workflow note: re-measuring requires *wipe → seed → fill* in that order, otherwise new aliases are stranded by `onConflictDoNothing` losing to aliases already claimed by NEW nodes from earlier loader runs. → commit `eaf46ac`.
- `GET /vacancies` silver feed shipped. New `apps/etl/src/vacancies/` module — Drizzle list query joins `sources`, `companies`, role/domain `nodes`, `rss_records` (link, publishedAt) + a second batched skills query keyed by vacancy id; the contract flattens FKs so the UI never sees opaque ids. Default behaviour surfaces only `status='VERIFIED'` for role / domain / skills and hides vacancies without a verified role; toggle via `?includeRoleless=1` and `?includeAllSkills=1`. New web page `apps/web/app/(investigation)/vacancies/` (Server Component) mirrors the `/monitoring` shell — `VacancyCard` headline shows `SENIORITY · role.name`, `FilterToggles` flips the two flags via `<Link>` and resets `offset`. Typed fetcher in `apps/web/lib/api/vacancies.ts` per ADR-0005 (no `libs/contracts` until the second integration). Open contract decisions D1–D13 carried forward. → commit `0340ecf`, [migration tracker](./migrations/vacancies-api.md).
- Frontend patterns reference (`md/engineering/FRONTEND.md`) — Next.js 16 + Server Components, `lib/api/` typed-fetcher conventions, no Apollo / Redux. Adapted from external reference, scoped to metahunt's actual stack, kept under the 200-line engineering soft cap. Linked from CLAUDE.md routing. Companion next-session brief at `todo/vacancies-api-specs.md` covers the most actionable followup — service + controller specs (F1+F2 in the vacancies-api tracker). → commit `58e96f8`.

## 2026-05-09

- Operator dashboard P1+P2+P3 + two polish rounds shipped on `feat/operator-dashboard-p3` (PR #12). New `(investigation)` group layout with sidebar (overview / pipeline / silver sections, mobile hamburger drawer, refresh in footer). `/dashboard` recomposed: KPI strip (ingests · records · failed · taxonomy queue with per-axis breakdown — SKILL the load-bearing number) + Latest-per-source w/ inline 7-run sparkline + activity stream of recent ingests. New `/sources` page joins `monitoringApi.sources()` with `taxonomyApi.coverage().bySource` client-side to surface % skill-verified per source without backend extension (D7). New `/taxonomy` page: coverage panel (axis stacked bars w/ health pills, fully-verified donut, skill-bucket histogram) + queue tabs (ROLE / SKILL / DOMAIN, search filter, scrolling list capped at 100/axis) + node drawer w/ fuzzy matches. Three SVG primitives at `apps/web/components/data/` (`Sparkline`, `StackedBar`, `Donut`) — theme-token strokes, no chart library. Old kitchen-sink `/monitoring` 308-redirected to `/dashboard`. Card polish landed in the second pass: `SeniorityBadge` (5-tone by level) replaces uppercase prefix in title, full UUIDs swapped for `CopyButton` + cleaner cross-links (vacancy → rss-record → ingest), locations truncate to 2 with `+N` overflow. Backend touch was minimal — `VacancyDto.rssRecordId` joined off the existing `rssRecords` query (one column + one type field). → [migration tracker](./migrations/_done/operator-dashboard.md).
- Roadmap: Stage 05 (silver layer) closed **2026-05-08** — silver pipeline stable, taxonomy curation Phase 1 backend in, `/vacancies` API MVP shipped (pipeline at the time: 180 ingests / 0 failed / 100% extraction). Stage 06 (extraction quality + operator surface) opens with the dashboard above as the entry surface; the BAML prompt-tuning thread ([`todo/baml-extraction-prompt-tuning.md`](../../todo/baml-extraction-prompt-tuning.md)) is the Stage 06 lever for bending the SKILL backlog down. → [`roadmap.md`](../roadmap.md).
