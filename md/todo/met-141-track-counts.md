# TODO — MET-141: track counts

Status: deferred on 2026-08-09. Do not include this in the current rollout.

When scheduled, make `track_counts` distinguish required nodes from optional
ones (`is_required`) and add the agreed velocity fields. Validate the count
semantics against canonical `positions` before changing the public track API.
