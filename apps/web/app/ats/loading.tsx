import { PageBody } from "@/ui/layout/PageBody";

export default function AtsLoading() {
  return (
    <PageBody>
      <div className="border border-border bg-bg-card p-6 font-mono text-xs text-text-muted">
        &gt; loading ATS postings and board health…
      </div>
    </PageBody>
  );
}
