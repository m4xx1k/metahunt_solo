# API base for Server-Component fetchers (lib/api/*). Points at the ETL.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Operator pages (the (investigation) route group) are gated by a Telegram
# session with the 'admin' role (ADMIN_TELEGRAM_IDS on the API). No separate
# operator auth provider — see md/runbook/auth.md.

# Archive PostHog client key. It remains separate during the v2 observation
# window; do not silently replace it with the v2 key.
# Use the project's Project API Key. Events route through the /ingest reverse
# proxy (next.config.ts) to dodge ad blockers — the PostHog region lives there,
# not here. EU project assumed; flip the hosts in next.config.ts for US.
NEXT_PUBLIC_POSTHOG_KEY=phc_xxx

# V2 client analytics is fail-closed: enable it only with a dedicated v2 key.
# Local verification must set test traffic so its events never enter human KPIs.
NEXT_PUBLIC_ANALYTICS_V2=false
NEXT_PUBLIC_ANALYTICS_TEST_TRAFFIC=true
NEXT_PUBLIC_POSTHOG_V2_KEY=

# Telegram login — the bot's @username, without the "@". Used to build the
# t.me/<bot>?start=login_<nonce> deep link. Empty → login disabled. See
# md/runbook/auth.md.
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=

# Same OAuth 2.0 Web client id the API verifies against. Google Identity
# Services needs it in the browser to mint the token. Empty → the Google
# button does not render and Telegram is the only sign-in.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
